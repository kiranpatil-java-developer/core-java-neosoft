package stream;

public class Test1 {
	public static void main(String[] args) {

//		String[] str = { "2", "5", "19", "17", "67", "32" };
//		List<Integer> collect = Arrays.stream(str).map(n -> Integer.valueOf(n)).sorted().collect(Collectors.toList());
//		System.out.println(collect);
//		List<String> collect2 = Arrays.stream(str).map(n -> Integer.valueOf(n)).sorted().map(c -> String.valueOf(c))
//				.collect(Collectors.toList());
//		System.out.println(collect2);

//		String str = "hello word";
//		String result = str.chars().mapToObj(c -> (char) c).filter(ch -> !"Aeiouaeiou".contains(String.valueOf(ch)))
//				.map(ch -> String.valueOf(ch)).collect(Collectors.joining());
//		System.out.println(result);
		
		

	}
}
